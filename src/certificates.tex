
\begin{tcbitemize}[
  raster columns=3, raster equal height=rows,
  blanker, cheat subtitle
]

  \tcbitem
    \tcbsubtitle{Check Certificate Details}
    \cmdhead{Check certificates}
    \begin{cheatcodebash}
    # View full certificate info (issuer, subject, SANs, dates)
    openssl x509 -in /etc/kubernetes/pki/apiserver.crt -text -noout
    # Check expiry date only
    openssl x509 -in /etc/kubernetes/pki/apiserver.crt -noout -enddate
    # Check issuer + subject
    openssl x509 -in /etc/kubernetes/pki/apiserver.crt -noout \
      -issuer -subject
    # Decode base64-encoded cert (e.g. from kubeconfig or Secret)
    echo "<base64-cert-data>" | base64 -d | openssl x509 -text -noout
    \end{cheatcodebash}


    \tcbsubtitle{Common Certificate Paths}
    \begin{tcolorbox}
        [
        skin=cheatboxtablesimple,
        before skip=0pt,
        after skip=0pt,
        tabularray={
        colspec={X[1.4,l]X[2,l]},
        columns={valign=t, colsep=0.4em},
        rows={rowsep=0pt, font=\sffamily\fontsize{6}{7}\selectfont},
        hline{2}={solid, wd=0.3pt, fg=gray!40},
        vline{2-3}={dash=solid, wd=0.3pt, fg=gray!30},
        row{1}={font=\sffamily\fontsize{6}{7}\selectfont\bfseries},
        }
        ]
      Component & Path \\
      API Server & \texttt{/etc/kubernetes/pki/apiserver.crt} \\
      API Server Key & \texttt{/etc/kubernetes/pki/apiserver-key.pem} \\
      CA Certificate & \texttt{/etc/kubernetes/pki/ca.crt} \\
      etcd Server & \texttt{/etc/kubernetes/pki/etcd/server.crt} \\
      Kubelet & \texttt{/var/lib/kubelet/pki/kubelet-client-current.pem} \\
        
    \end{tcolorbox}

    \tcbsubtitle{Renew \& Manage (kubeadm)}
    \begin{cheatcodebash}
    # Check expiration of all cluster certificates
    kubeadm certs check-expiration
    # Renew all certificates
    kubeadm certs renew all
    # Renew a specific certificate
    kubeadm certs renew apiserver
    \end{cheatcodebash}

    \warning{After renewing certs, restart control-plane Pods (or move manifests out of \texttt{/etc/kubernetes/manifests/} and back).}

    \begin{cheatcodebash}
    # Verify cert matches key (modulus must match):
    openssl x509 -in cert.crt -noout -modulus | md5sum
    openssl rsa  -in cert.key -noout -modulus | md5sum
    # Symptoms: "x509: certificate has expired" or
    # "Unable to connect to the server"
    # docs: search "manage tls certificates"
    \end{cheatcodebash}

  \tcbitem
    \tcbsubtitle{User Certificate Signing Flow}
    {\fontsize{6.5}{7.5}\selectfont\sffamily
    Grant a user (or CI service) API access via a client certificate signed by the cluster CA — no ServiceAccount token needed.\par}

    \cmdhead{1. Generate private key \& CSR}
    \begin{cheatcodebash}
    openssl genrsa -out jane.key 2048
    openssl req -new -key jane.key -subj "/CN=jane/O=dev-team" -out jane.csr
    \end{cheatcodebash}

    \cmdhead{2. Create Kubernetes CSR object}
    \begin{cheatcodebash}
    cat <<EOF | kubectl apply -f -
    apiVersion: certificates.k8s.io/v1
    kind: CertificateSigningRequest
    metadata:
      name: jane
    spec:
      request: $(cat jane.csr | base64 | tr -d '\n')
      signerName: kubernetes.io/kube-apiserver-client
      usages: ["client auth"]
    EOF
    \end{cheatcodebash}

    \cmdhead{3. Admin approves the CSR}
    \begin{cheatcodebash}
    kubectl certificate approve jane
    kubectl get csr jane    # STATUS: Approved,Issued
    \end{cheatcodebash}

    \cmdhead{4. Retrieve the signed certificate}
    \begin{cheatcodebash}
    kubectl get csr jane -o jsonpath='{.status.certificate}' \
      | base64 -d > jane.crt
    \end{cheatcodebash}

    \cmdhead{5. Add user credentials to default kubeconfig (\textasciitilde/.kube/config)}
    \begin{cheatcodebash}
    kubectl config set-credentials jane \
      --client-certificate=jane.crt --client-key=jane.key \
      --embed-certs=true
    kubectl config set-context jane-ctx \
      --cluster=kubernetes --user=jane
    kubectl config use-context jane-ctx
    \end{cheatcodebash}
    \cmdhead{6. Grant permissions via RBAC}
    \begin{cheatcodebash}
    # Role (namespace-scoped) or ClusterRole:
    k create role jane-role --verb=get,list,watch \
      --resource=pods,deployments -n dev
    # Bind role to user (CN from cert):
    k create rolebinding jane-bind --role=jane-role \
      --user=jane -n dev
    # Or bind to group (O from cert):
    k create clusterrolebinding dev-view \
      --clusterrole=view --group=dev-team
    # Verify:
    k auth can-i list pods -n dev --as=jane
    \end{cheatcodebash}
    \note{\texttt{CN} becomes the username, \texttt{O} becomes the group. RBAC matches RoleBindings for \texttt{user:} or \texttt{group:} subjects.}


  \tcbitem
    \tcbsubtitle{Setup Separate Kubeconfig}
    {\fontsize{6.5}{7.5}\selectfont\sffamily
    Step 5 adds credentials to your admin \texttt{\textasciitilde/.kube/config}. To create a \textbf{standalone} kubeconfig file (portable, to hand off to the user), copy the admin config as a template, strip existing users/contexts, then add the new user's credentials.\par}

    \cmdhead{6a. Create kubeconfig from template}
    \begin{cheatcodebash}
    # Copy admin config as base (keeps cluster + CA info)
    cp ~/.kube/config jane.config
    KC="KUBECONFIG=jane.config"
    eval $KC k config unset users.default
    eval $KC k config delete-context default
    eval $KC k config unset current-context
    \end{cheatcodebash}

    \cmdhead{6b. Set user credentials (embed certs)}
    \begin{cheatcodebash}
    eval $KC k config set-credentials jane \
      --client-certificate=jane.crt \
      --client-key=jane.key \
      --embed-certs=true
    \end{cheatcodebash}

    \cmdhead{6c. Set context and activate}
    \begin{cheatcodebash}
    eval $KC k config set-context default \
      --cluster=default --user=jane
    eval $KC k config use-context default
    \end{cheatcodebash}

    \cmdhead{Shortcut: use variables for CSR YAML}
    \begin{cheatcodebash}
    CSR_DATA=$(base64 jane.csr | tr -d '\n')
    CSR_USER=jane
    # Then use $CSR_DATA and $CSR_USER in the YAML
    \end{cheatcodebash}

    \note{\texttt{--embed-certs=true} inlines cert data into the kubeconfig — the file is self-contained and portable.}


  \end{tcbitemize}

\vspace{1ex}

\tcbsubtitle{How Certificates, Users, Groups \& RBAC Connect}

\begin{tcbitemize}[
  raster columns=3, raster equal height=rows,
  blanker, cheat subtitle
]

  % === Col 1: Intro + CN/O table + full chain =================================
  \tcbitem

    {\fontsize{6.5}{7.5}\selectfont\sffamily
    Kubernetes has \textbf{no User object}. Identity comes from \textbf{outside} (certificates, OIDC tokens, etc.). The API server \emph{extracts} identity from the credential and matches it against RBAC rules.\par}

    \vspace{0.5ex}

    \begin{tcolorbox}
        [
        skin=cheatboxtablesimple,
        before skip=0pt,
        after skip=0pt,
        tabularray={
        colspec={X[1.2,l]X[1.2,l]X[2.5,l]},
        columns={valign=t, colsep=0.4em},
        rows={rowsep=0.5pt, font=\sffamily\fontsize{6}{7}\selectfont},
        hline{2}={solid, wd=0.3pt, fg=gray!40},
        vline{2-3}={dash=solid, wd=0.3pt, fg=gray!30},
        row{1}={font=\sffamily\fontsize{6}{7}\selectfont\bfseries},
        }
        ]
      Certificate Field & K8s Identity & Example \\
      \texttt{CN} (Common Name) & Username & \texttt{/CN=jane} → user \texttt{jane} \\
      \texttt{O} (Organization) & Group(s) & \texttt{/O=dev-team} → group \texttt{dev-team} \\
      Multiple \texttt{O} & Multiple groups & \texttt{/O=dev-team/O=admins} → both groups \\

    \end{tcolorbox}

  % === Col 2: mTLS explanation + RBAC binding =================================
  \tcbitem

    {\fontsize{6}{7}\selectfont\sffamily
    \textbf{Why the private key never leaves the user:} The mTLS handshake is a cryptographic proof of possession. The API server challenges the user; only the holder of the private key can sign correctly. The key itself is never transmitted. This is \textbf{mutual TLS}: both sides authenticate (server cert + client cert).\par}

    \vspace{0.5ex}

    \cmdhead{RBAC binding examples}
    \begin{cheatcodebash}
    # Bind to user (matches CN)
    k create rolebinding jane-edit --clusterrole=edit \
      --user=jane -n dev

    # Bind to group (matches O)
    k create clusterrolebinding dev-view \
      --clusterrole=view --group=dev-team
    \end{cheatcodebash}

    \cmdhead{Verify identity extracted from cert}
    \begin{cheatcodebash}
    # "Who am I?" — shows username and groups from cert
    k auth whoami
    # Test permissions as that user/group
    k auth can-i list pods --as=jane
    k auth can-i list pods --as-group=dev-team
    \end{cheatcodebash}

    \important{Changing a user's name or group requires a \textbf{new certificate} — you cannot revoke or modify a cert field.}

  % === Col 3: mTLS Handshake sequence diagram ==================================
  \tcbitem

    \cmdhead{mTLS Handshake (every kubectl call)}
    {\fontsize{4.5}{5.5}\selectfont\sffamily\color{gray!70!black}
    Standard TLS = encrypted channel (browser ↔ web server).\\
    \textbf{mTLS} = encrypted channel \textbf{+ client identity} via certificate.\par}
    \vspace{0.3ex}
    \begin{center}
    \begin{tikzpicture}[
      >=Stealth,
      every node/.style={font=\sffamily\fontsize{4.5}{5.5}\selectfont},
      snote/.style={draw=gray!30, fill=yellow!8, rounded corners=1pt, inner sep=1.5pt,
        font=\sffamily\fontsize{3.5}{4.5}\selectfont\itshape, align=center},
      cnote/.style={draw=teal!30, fill=teal!5, rounded corners=1pt, inner sep=1.5pt,
        font=\sffamily\fontsize{3.5}{4.5}\selectfont, align=center},
      phlbl/.style={font=\sffamily\fontsize{4}{5}\selectfont\bfseries,
        fill=k8sblue!8, draw=k8sblue!30, rounded corners=1pt, inner sep=1pt},
    ]
      % --- Participants ---
      \node[font=\sffamily\fontsize{5}{6}\selectfont\bfseries,
        draw=teal!60, fill=teal!10, rounded corners=2pt, inner sep=2pt] (client) {kubectl};
      \node[font=\sffamily\fontsize{5}{6}\selectfont\bfseries,
        draw=k8sblue!60, fill=k8sblue!10, rounded corners=2pt, inner sep=2pt,
        right=3cm of client] (server) {API Server};

      % --- What each side holds ---
      \node[snote, text width=1.5cm, below=0.08cm of client]
        (chas) {\textbf{Has:}\\ jane.key \textit{(private)}\\ jane.crt \textit{(cert)}\\ ca.crt \textit{(CA trust)}};
      \node[snote, text width=1.6cm, below=0.08cm of server]
        (shas) {\textbf{Has:}\\ server.key \textit{(private)}\\ server.crt \textit{(cert)}\\ ca.crt \textit{(CA trust)}};

      \def\ystart{-0.25}
      \def\ystep{0.42}

      % === Standard TLS part === (label placed further left to avoid overlap)
      \pgfmathsetmacro{\yph}{\ystart - 0.8*\ystep}
      \node[phlbl, anchor=east] at ([yshift=\yph cm, xshift=-0.25cm]chas.south) {\rotatebox{90}{\tiny standard TLS}};

      % 1. Client Hello
      \draw[->, semithick, teal!60]
        ([yshift=\ystart cm]chas.south) -- ([yshift=\ystart cm]shas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {1. ClientHello (ciphers, random)};

      % 2. Server Hello + Cert
      \pgfmathsetmacro{\ypos}{\ystart - \ystep}
      \draw[->, semithick, k8sblue!60]
        ([yshift=\ypos cm]shas.south) -- ([yshift=\ypos cm]chas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {2. ServerHello + server.crt};

      % Client verifies server cert
      \pgfmathsetmacro{\ypos}{\ystart - 1.6*\ystep}
      \node[snote, text width=1.7cm]
        at ([yshift=\ypos cm]chas.south)
        {verify server.crt signed by \textbf{ca.crt}};

      % 3. Key exchange
      \pgfmathsetmacro{\ypos}{\ystart - 2.3*\ystep}
      \draw[->, semithick, teal!60]
        ([yshift=\ypos cm]chas.south) -- ([yshift=\ypos cm]shas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {3. Key exchange → shared session key};

      % Session key note
      \pgfmathsetmacro{\ypos}{\ystart - 2.85*\ystep}
      \node[cnote, text width=3.2cm, draw=green!30, fill=green!5]
        at ([yshift=\ypos cm]$ (chas.south)!0.5!(shas.south) $)
        {Both sides now share a \textbf{symmetric session key}. All traffic encrypted. (Same as browser ↔ web server.)};

      % === mTLS addition ===
      \pgfmathsetmacro{\ypos}{\ystart - 3.8*\ystep}
      \node[phlbl, anchor=east] at ([yshift=\ypos cm, xshift=-0.15cm]chas.south) {\rotatebox{90}{\tiny mTLS addition}};

      % 4. CertificateRequest
      \draw[->, semithick, k8sblue!60]
        ([yshift=\ypos cm]shas.south) -- ([yshift=\ypos cm]chas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {4. CertificateRequest};

      % 5. Client Certificate
      \pgfmathsetmacro{\ypos}{\ystart - 4.6*\ystep}
      \draw[->, semithick, teal!60]
        ([yshift=\ypos cm]chas.south) -- ([yshift=\ypos cm]shas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {5. jane.crt (public key + CN/O)};

      % 5 note
      \pgfmathsetmacro{\ypos}{\ystart - 5.05*\ystep}
      \node[snote, text width=3.2cm]
        at ([yshift=\ypos cm]$ (chas.south)!0.5!(shas.south) $)
        {Server has client's \textbf{public key} + claimed identity — but doesn't yet know if client truly owns it.};

      % 6. CertificateVerify
      \pgfmathsetmacro{\ypos}{\ystart - 6*\ystep}
      \draw[->, semithick, teal!70, very thick]
        ([yshift=\ypos cm]chas.south) -- ([yshift=\ypos cm]shas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont\bfseries]
        {6. CertificateVerify};

      % CertificateVerify explanation
      \pgfmathsetmacro{\ypos}{\ystart - 6.8*\ystep}
      \node[cnote, text width=3.2cm]
        at ([yshift=\ypos cm]$ (chas.south)!0.5!(shas.south) $)
        {\textbf{Sent:} hash of handshake, signed with \textbf{jane.key}.\\[1pt]
        \textbf{Server:} decrypts with public key from jane.crt. Match → client owns the key.};

      % Server verifies cert trust + extracts identity
      \pgfmathsetmacro{\ypos}{\ystart - 8*\ystep}
      \node[snote, text width=1.6cm]
        at ([yshift=\ypos cm]shas.south)
        {1) jane.crt signed by \textbf{ca.crt}?\\
        2) CertVerify valid?\\
        3) extract CN→user, O→group};

      % 7. Finished
      \pgfmathsetmacro{\ypos}{\ystart - 9.2*\ystep}
      \draw[<->, semithick, green!50!black]
        ([yshift=\ypos cm]chas.south) -- ([yshift=\ypos cm]shas.south)
        node[midway, above, font=\sffamily\fontsize{3.5}{4.5}\selectfont]
        {7. Encrypted + authenticated};

      % Bottom note
      \pgfmathsetmacro{\ypos}{\ystart - 9.8*\ystep}
      \node[draw=k8sblue!30, fill=k8sblue!5, rounded corners=1pt, inner sep=1.5pt,
        font=\sffamily\fontsize{3.5}{4.5}\selectfont, text width=3.2cm, align=center]
        (bottomnote) at ([yshift=\ypos cm]$ (chas.south)!0.5!(shas.south) $)
        {Server knows: \textbf{who} (CN=jane), \textbf{group} (O=dev-team). RBAC applied.};

      % --- Lifelines (drawn last, ending at bottom note) ---
      \draw[gray!40, dashed] (chas.south) -- (chas.south |- bottomnote.south);
      \draw[gray!40, dashed] (shas.south) -- (shas.south |- bottomnote.south);

    \end{tikzpicture}
    \end{center}

  \end{tcbitemize}

