% ----------------------------------------------------------------------------------------------------------------------
% Kubernetes Summary Sheet
% ----------------------------------------------------------------------------------------------------------------------

% User guides
% Latex: https://ftp.agdsn.de/pub/mirrors/latex/dante/macros/latex/base/usrguide.pdf
% Tabularray: https://mirror.physik.tu-berlin.de/pub/CTAN/macros/latex/contrib/tabularray/tabularray.pdf
% TColorbox: https://mirror.clientvps.com/CTAN/macros/latex/contrib/tcolorbox/tcolorbox.pdf
% Posterbox Tutorial: https://mirror.clientvps.com/CTAN/macros/latex/contrib/tcolorbox/tcolorbox-tutorial-poster.pdf#[0,{%22name%22:%22Fit%22}]
% Xcolor: https://mirror.clientvps.com/CTAN/macros/latex/contrib/xcolor/xcolor.pdf
% TikZ: https://tikz.dev/
% Geometry: https://ftp.gwdg.de/pub/ctan/macros/latex/contrib/geometry/geometry.pdf
% fontspec: https://texdoc.org/serve/fontspec/0
% fancyhdr: https://ftp.fau.de/ctan/macros/latex/contrib/fancyhdr/fancyhdr.pdf
% biblatex: https://distrib-coffee.ipsl.jussieu.fr/pub/mirrors/ctan/macros/latex/contrib/biblatex/doc/biblatex.pdf
% amsmath: https://www.latex-project.org/help/documentation/amsldoc.pdf

% ----------------------------------------------------------------------------------------------------------------------

\documentclass[8pt]{extarticle}


% --- Hyphenation Rules --------------------------------------------------------
\usepackage[USenglish]{babel}

% --- Fonts --------------------------------------------------------------------

% Load system fonts via fontspec (requires XeLaTeX or LuaLaTeX)
% Note: fontenc is not needed with fontspec + LuaLaTeX
\usepackage{fontspec}
\setmainfont{FreeSans}[Scale=0.95]
\setsansfont{FreeSans}[Scale=0.95]
\setmonofont{Source Code Pro}[
  Contextuals=Alternate,
  Scale=0.80
]

% Unicode math
\usepackage{unicode-math}
\setmathfont{Latin Modern Math}

% font for the header
\newfontfamily\headerfont{QTHelvet-Black}

% Emoji support
\usepackage{emoji}
\setemojifont{Twemoji Mozilla}

% --- Page layout --------------------------------------------------------------
\usepackage[
    nomarginpar,
    a4paper,
    landscape,
    right=0.25cm,
    left=0.25cm,
    top=1cm,
    bottom=0.65cm,
    headsep=0.1cm,
    headheight=0.75cm,
    footskip=0.35cm,
]{geometry}


\usepackage{fancyhdr}


% --- TColorbox ---------------------------------------------------------------
% minted v3: 'fastfirst' uses cache when available, highlights on first pass.
% Avoids re-running Pygments on unchanged code blocks.
\PassOptionsToPackage{highlightmode=fastfirst}{minted}
\usepackage{tcolorbox}
\tcbuselibrary{skins,raster,listings,breakable,minted,poster}

% --- Tables -------------------------------------------------------------------
\usepackage{tabularray}

% --- Color Names -------------------------------------------------------------
\usepackage[svgnames]{xcolor}


% --- Graphics -----------------------------------------------------------------
\usepackage{tikz}
\usetikzlibrary{calc, trees, graphs, fit, matrix, shapes, arrows.meta, positioning, decorations.pathreplacing, calligraphy}

\usepackage{graphicx}
\usepackage{svg}
\graphicspath{{src/figures/}{src/icons/}{src/logos/}{src/assets/}{./figures/}{./icons/}{./logos/}{./assets/}}

\usepackage{bytefield}

% --- URL and href -----------------------------------------------------------
\colorlet{citecolor}{black}
\colorlet{linkcolor}{black}
\colorlet{urlcolor}{black}
\usepackage[
  bookmarks=true,
  breaklinks=true,
  pdfborder={0 0 0},
  citecolor=citecolor,
  linkcolor=linkcolor,
  urlcolor=urlcolor,
  colorlinks=true,
  linktocpage=false,
  hyperindex=true,
  linkbordercolor=white]{hyperref}

% --- Math Environments --------------------------------------------------------
\usepackage{amsmath}
\usepackage{nicematrix}
\NiceMatrixOptions{cell-space-limits = 1pt}
\usepackage[detect-all, locale=US, group-separator={\,}]{siunitx}

% --- Bibliography (disabled — cheatsheet has no citations) --------------------
% \usepackage[autostyle=true]{csquotes}
% \usepackage[backend=biber,style=numeric,sorting=nyt]{biblatex}
% \addbibresource{../bibliography/references.bib}

% --- Helpers -----------------------------------------------------------------
\usepackage{lipsum}
\usepackage{enumitem}

% --- Input path (needed because tcbposter writes content to out/main.tcbtemp) -
\makeatletter
\def\input@path{{src/}{./}}
\makeatother


% --- Default options ---------------------------------------------------------
\pagestyle{fancy}

% --- Version (must match git release tag) ------------------------------------
\newcommand{\cheatsheetversion}{v0.2.0}

\definecolor{k8sblue}{RGB}{50,108,229}   % Kubernetes CI primary blue #326CE5
\newcommand{\primarycolor}{k8sblue}
\newcommand{\secondarycolor}{Navy}
\newcommand{\graycolor}{SlateGray}
\newcommand{\maincolspan}{4}


% ---- Macros --------------------------------------------------------------------

% --- TColorbox Skins ----------------------------------------------------------

% subtitle style extracted for reusability
\tcbset{
  cheat subtitle/.style={
    subtitle style={
      left=0.2em,
      top=0.4mm, 
      bottom=0mm, 
      boxrule=0.2pt, 
      boxsep=0.1mm,
      colback=\primarycolor!30!white,
      colupper=\primarycolor!50!black,
      fontupper=\sffamily\bfseries\small,
      coltext=\primarycolor!10!black,
      height=1.1\baselineskip,
    }
  }
}

% Skin for cheat section boxes
\tcbsubskin{cheatboxskin}{standard}{
    size=title,
    left=0.2em,
    right=0.2em,
    bottom=0.2em,
    arc=0.25mm,
    colback=white,
    colframe=\primarycolor!70!black,
    coltitle=\primarycolor!5!white,
    colbacktitle=\primarycolor,
    fonttitle=\sffamily\bfseries,
    toptitle=0mm,
    bottomtitle=0mm,
    fontupper=\sffamily\small,
    fontlower=\sffamily\small,
    cheat subtitle
    % subtitle style={top=0.4mm, bottom=0mm, boxrule=0.2pt, boxsep=0.1mm,
    %     colback=\primarycolor!30!white,
    %     colupper=\primarycolor!50!black,
    %     fontupper=\sffamily\bfseries\small,
    %     coltext=\primarycolor!10!black,
    %     height=1.1\baselineskip,
    %   },
}


% Skin for simple tables inside cheat boxes
\tcbsubskin{cheatboxtablesimple}{empty}{
    size=minimal,
    fontupper=\sffamily\small,
    fontlower=\sffamily\small,
    before skip=0.2\baselineskip,
    after skip=0.2\baselineskip,
    colback=white,
    colframe=gray,
    frame empty,
}

% --- Code listing environment -------------------------------------------------
% fontsize= in minted/fancyvrb expects a single parameterless command
\newcommand{\cheatcodefontsize}{\fontsize{6.5}{7.5}\selectfont}

% Shared tcolorbox style for all cheatcodebash/yaml/tex variants
\tcbset{cheatcodestyle/.style={%
  listing only,%
  listing engine=minted,%
  minted options={fontsize=\cheatcodefontsize,breaklines,autogobble, escapeinside=!!},%
  colback=k8sblue!3!white,%
  colframe=k8sblue!22!white,%
  boxrule=0.3pt,%
  size=minimal,%
  left=5pt, right=1pt, top=1.5pt, bottom=1pt,%
  arc=1.5pt,outer arc=1.5pt,%
  before skip=1pt,%
  after skip=4pt,%
}}

% Shell / bash snippets
\newtcblisting{cheatcodebash}{%
  cheatcodestyle,%
  minted language=bash,%
}

% Important bash snippets — left accent stripe + stronger background
\newtcblisting{cheatcodebashbold}{%
  cheatcodestyle,%
  minted language=bash,%
  colback=k8sblue!9!white,%
  colframe=k8sblue!50!white,%
  borderline west={2.5pt}{0pt}{k8sblue!70!white},%
  after skip=4pt,%
}

% YAML manifests
\newtcblisting{cheatcodeyaml}{%
  cheatcodestyle,%
  minted language=yaml,%
}

% Like cheatcodebash but without the listing engine — LaTeX commands work inside (useful for \textcolor etc.)
\newtcolorbox{cheatcodetex}{%
  colback=k8sblue!3!white,%
  colframe=k8sblue!22!white,%
  boxrule=0.3pt,%
  size=minimal,%
  left=5pt, right=2pt, top=1.5pt, bottom=1pt,%
  arc=1.5pt,%
  before skip=1pt,%
  after skip=6pt,%
}


%textmarker style from colorbox doc
\tcbset{textmarker/.style={%
        outer arc=0mm,left=5mm,right=2mm,top=3pt,bottom=5pt,
size=small,skin=bicolor,
    fontupper=\fontsize{9}{8}\selectfont,
    fontlower=\fontsize{5}{6}\selectfont,
    righthand width=0.85\linewidth,sidebyside,sidebyside gap=3pt,
    lower separated=false,
        }}


% define new colorboxes
\newtcolorbox{hintBox}{textmarker,
    colframe=yellow!30!white,%
    borderline west={6pt}{0pt}{yellow!30!white},
    colback=yellow!15!white,
    colbacklower=yellow!5!white,
    }
\newtcolorbox{importantBox}{textmarker,
    colframe=red!30!white,%
    borderline west={6pt}{0pt}{red!30!white},
    colback=red!15!white,
    colbacklower=red!5!white,
    }
\newtcolorbox{noteBox}{textmarker,
    colframe=green!30!white,%
    borderline west={6pt}{0pt}{green!30!white},
    colback=green!15!white,
    colbacklower=green!5!white,
    }

% define commands for easy access
\newcommand{\note}[1]{\begin{noteBox}\emoji{information} \tcblower #1\end{noteBox}}
\newcommand{\warning}[1]{\begin{hintBox} \emoji{warning} \tcblower #1 \end{hintBox}}
\newcommand{\important}[1]{\begin{importantBox}\emoji{stop-sign} \tcblower #1 \end{importantBox}}


\newcommand{\cmdhead}[1]{{
  \fontsize{6}{7}\selectfont
  \vspace{0.5ex}
  \textbf{#1}
}}

% ----------------------------------------------------------------------------------------------------------------------
% Header and Footer Settings
% ----------------------------------------------------------------------------------------------------------------------

\fancyhead[L]{
    \huge\raisebox{-0.1\height}{\raisebox{-0.1cm}{\includegraphics[height=0.55cm]{Kubernetes_logo_without_workmark.svg.png}}~\headerfont{\textcolor{\primarycolor}{Kubernetes}} Summary Sheet}
}
\fancyhead[C]{\small\sffamily\cheatsheetversion\enspace\textbar\enspace K8s v1.25--v1.35}
\fancyhead[R]{\thepage}
\fancyfoot[L]{}
\fancyfoot[C]{}
\fancyfoot[R]{}



% ----------------------------------------------------------------------------------------------------------------------
% Main Document
% ----------------------------------------------------------------------------------------------------------------------

\begin{document}

% === Page 1: Cluster Architecture, Setup & Cheats, Pods & Deployments ====
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === Cluster Architecture ===================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title=Cluster Architecture]
        {name=clusterarchitecture, column=1, between=top and bottom, span=4}
        \input{cluster_architecture.tex}
    \end{posterboxenv}

    % === Pods ====================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Pods}]
        {name=pods, column=5, between=top and bottom, span=3}
        \input{pods.tex}
    \end{posterboxenv}

    % === Deployments ====================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Deployments}]
        {name=deployments, column=8, below=top, span=3}
        \input{deployments.tex}
    \end{posterboxenv}

    % === Namespaces =========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Namespaces}]
        {name=namespaces, column=8, between=deployments and bottom, span=3}
        \input{namespaces.tex}
    \end{posterboxenv}


    % === Labels & Selectors ====================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Labels \& Selectors}]
        {name=labelsselectors, column=11, below=top, span=2}
        \input{labels_selectors.tex}
    \end{posterboxenv}

    % === Context =========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Context}]
        {name=context, column=11, below=labelsselectors, span=2}
        \input{context.tex}
    \end{posterboxenv}

    % === Shell Helper ================================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Shell Helper}]
        {name=shellhelper, column=11, below=context, span=2}
        \input{shell_helper.tex}
    \end{posterboxenv}

    % === Tools ====================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Tools}]
        {name=tools, column=11, between=shellhelper and bottom, span=2}
        \input{tools.tex}
    \end{posterboxenv}




\end{tcbposter}

\pagebreak

% === Page 2: Scheduling, Jobs/DaemonSets, ConfigMaps & Secrets ============
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === Scheduling ============================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title=Scheduling]
        {name=scheduling, column=1, between=top and bottom, span=9}
        \input{scheduling.tex}
    \end{posterboxenv}

    % === Jobs =========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Jobs}]
        {name=jobs, column=10, below=top, span=3}
        \input{jobs.tex}
    \end{posterboxenv}

    % === Typical Limits =========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Typical Limits}]
        {name=limits, column=10, between=jobs and bottom, span=3}
        \input{limits.tex}
    \end{posterboxenv}



\end{tcbposter}

\pagebreak

% === Page 3: Services & Networking, Storage, Autoscaling & Probes =========
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === Services & Networking =================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Services \& Networking}]
        {name=networking, column=1, between=top and bottom, span=9}
        \input{services_networking.tex}
    \end{posterboxenv}


    % === Autoscaling ==================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Autoscaling}]
        {name=autoscaling, column=10, below=top, span=3}
        \input{autoscaling.tex}
    \end{posterboxenv}


\end{tcbposter}


\pagebreak

% === Page 4: XXX  =========
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === ConfigMaps & Secrets ==================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={ConfigMaps \& Secrets}]
        {name=configmaps, column=1, below=top, span=9}
        \input{configmaps_secrets.tex}
    \end{posterboxenv}

    % === Security =======================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Security}]
        {name=security, column=1, between=configmaps and bottom, span=9}
        \input{security.tex}
    \end{posterboxenv}

    % === Helm, Kustomize & Extensions ==========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Helm, Kustomize \& Extensions}]
        {name=helm, column=10, between=top and bottom, span=3}
        \input{helm_kustomize.tex}
    \end{posterboxenv}




\end{tcbposter}



\pagebreak


% === Page 5: .... =============
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]


    % === Storage ===============================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title=Storage]
        {name=storage, column=1, between=top and bottom, span=6}
        \input{storage.tex}
    \end{posterboxenv}

    % === StatefulSets ===============================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title=StatefulSets]
        {name=statefulsets, column=7, below=top, span=6}
        \input{statefulsets.tex}
    \end{posterboxenv}



\end{tcbposter}


\pagebreak


% === Page 6: .... =============
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === Certificates =======================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Certificates}]
        {name=certificates, column=1, between=top and bottom, span=9}
        \input{certificates.tex}
    \end{posterboxenv}



    % === Metrics Server & Probes ==========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Metrics Server \& Probes}]
        {name=probes, column=10, between=top and bottom, span=3}
        \input{probes.tex}
    \end{posterboxenv}



\end{tcbposter}



\pagebreak


% === Page 8: .... =============
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]

    % === Pod Disruptions Budgets =========================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Pod Disruption Budget}]
        {name=pdb, column=1, between=top and bottom, span=3}
        \input{pdb.tex}
    \end{posterboxenv}


    % === JsonPath ==========================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={JSONPath \& Output}]
        {name=jsonpath, column=4, below=top, span=3}
        \input{jsonpath.tex}
    \end{posterboxenv}

    % === Downward API ======================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Downward API}]
        {name=downwardapi, column=4, between=jsonpath and bottom, span=3}
        \input{downward_api.tex}
    \end{posterboxenv}

    % === RBAC =======================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={RBAC}]
        {name=rbac, column=7, between=top and bottom, span=6}
        \input{rbac.tex}
    \end{posterboxenv}

\end{tcbposter}


\pagebreak

% === Page X: xxx =============
\begin{tcbposter}[
        poster = {spacing=0.6em, columns=12, height=\textheight},
        boxes = {},
        coverage={left=0pt, right=0pt, top=0pt, bottom=0pt},
    ]


    % === Troubleshooting =======================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title=Troubleshooting]
        {name=troubleshooting, column=1, between=top and bottom, span=6}
        \input{troubleshooting.tex}
    \end{posterboxenv}


    % === Typical Tasks ==========================================================
    \begin{posterboxenv}[skin=cheatboxskin, adjusted title={Typical Tasks}]
        {name=ckatasks, column=7, between=top and bottom, span=6}
        \input{typical_tasks.tex}
    \end{posterboxenv}


\end{tcbposter}


\end{document}
